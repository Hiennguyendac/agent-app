/**
 * TaskType describes the kind of work a task belongs to.
 *
 * For the first MVP, we support only one real path: Growth.
 * More task types can be added later when the system grows.
 */
export type TaskType = "growth";
